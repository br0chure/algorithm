---
id: 0700
title: 二叉搜索树中的搜索
difficulty: 简单
url: https://leetcode.cn/problems/search-in-a-binary-search-tree/
tags: [bst]
---

# 二叉搜索树中的搜索

## 题面

给定 BST 的根节点 `root` 和一个整数值 `val`，找到 BST 中节点值等于 `val` 的节点，**返回以该节点为根的子树**。如果节点不存在，返回 `nil`。

样例：`root = [4,2,7,1,3]`、`val = 2` → 返回节点 2 及其子树 `[2,1,3]`。

约束：节点数 `1 ≤ n ≤ 5000`，`1 ≤ Node.val, val ≤ 10^7`，节点值唯一。

## 思路

**Pattern：bst · 左小右大递归** —— Type A（返回值即答案）。

### 三问

| 问题 | 答案 |
|---|---|
| 1. 递归返回什么？ | `*TreeNode`，找到的节点 OR `nil` |
| 2. base case？ | `root == nil` → `nil` |
| 3. 当前节点做什么？ | 比较 `val` 与 `root.Val`：相等返回 root；小于走左；大于走右 |

**关键**：BST 性质保证**每次只搜一边**——`val < root.Val` 时 val 必然不在右子树。

## 完整代码

```go
func searchBST(root *TreeNode, val int) *TreeNode {
    if root == nil {
        return nil
    }
    if root.Val == val {
        return root
    }
    if root.Val > val {
        return searchBST(root.Left, val)
    }
    return searchBST(root.Right, val)
}
```

## 复杂度

- **时间：O(h)**——每层只下沉一边，最多走树高 h 步。平衡 BST 是 O(log n)，链状最坏 O(n)。
- **空间：O(h)**——递归栈深度等于路径长度。

# 刷题工作流

定义"从遇到一道题到归档完成"的标准流程。
搭配 [patterns/README.md](patterns/README.md)（Pattern 工作流）和 [REVIEW.md](REVIEW.md)（复习节奏）一起使用。

---

## 流程

1. **LC 网页写题**：直接在 LeetCode 网页运行 / 提交。本仓库不放本地单测，AC 即过。
2. **当面分析复杂度**：AC 后**先带用户一起推导时间 + 空间**（两个都要、讲清推导），再往下走——**这一步不能跳，不是归档时默默写掉**。
3. **本地归档**：把代码 + 思路 + 复杂度写进 `problems/<系列>/XXXX 中文标题.md`。
4. **同步 git**：`git add` + `git commit` + `git push`。

---

## 开新系列

切入一个新问题域（如回溯、动态规划）前，先读对应的经典题目素材，作为地图与题源：

`~/Documents/go求职/经典题目/<类型>.md` —— 含理论基础、通用模板、按子类型分章的精选题 + 注意点。

据此产出三件套：

1. **INDEX 分组骨架**：按素材的小节给系列分组（如回溯 = 组合 / 子集 / 排列 / 棋盘），列精选题号，标 ❌ 未刷。
2. **`patterns/<系列>/` 子目录**：系列名用中文（`二叉树/`、`回溯/` …），与 INDEX 分组对应。pattern 文档**随做题逐步沉淀**——做题前不预先抄模板，避免剧透。
3. **刷题路线**：从精选题里定先后顺序，先易后难。

> 经典题目素材里的代码是 Java / 伪码，归档时统一翻成 Go。

---

## 复杂度分析要求

**每题 AC 后先和用户当面过一遍**（流程第 2 步），再写进归档。不能只写一行 `O(n)`，要把推导讲清楚——

### 时间
- 每个节点 / 元素被访问几次？有没有嵌套？
- 最坏 / 平均 / 摊销分别是什么？

### 空间
- 哪些数据结构占空间（队列、栈、递归栈、辅助哈希）？
- 峰值占用是多少？为什么是这个量级？给出推导（例：「满二叉树最后一层 ≈ n/2 个节点」）。
- 输出本身的 `result` 是否计入？

> 一眼能看出来的（如双指针 O(n)/O(1)）可以简写；
> **只要不是一眼能看出来，就要给推导过程**，便于将来翻看时复盘。

---

## 每题 .md 模板

文件命名：`XXXX 中文标题.md`（4 位题号 + 一个空格 + 中文标题）。

~~~markdown
---
id: 0001
title: 两数之和
difficulty: 简单
url: https://leetcode.cn/problems/two-sum/
tags: [hash_map, array]
---

# 两数之和

## 题面
（题目描述或链接）

## 思路
...

## 完整代码

```go
func twoSum(nums []int, target int) []int { ... }
```

## 复杂度
- 时间：O(n)，<推导：每个节点访问几次、有无嵌套>
- 空间：O(n)，<推导：哪个数据结构吃空间、峰值多少、为什么>
~~~

---

## 一题多解

在同一个 .md 内追加「## 备用解 / 优化解」段落，每段落含独立代码块。

> ⚠️ 不归档"劣于主解"的版本（如全局变量 vs 闭包、闭包更弱的写法）。
> 通用反例只入 pattern 心法，题目目录默认只留主解。

---

## 不做的事

- ❌ 不写本地 `*_test.go` 单元测试（LC 提交即测试）
- ❌ 不创建独立的 `.go` 文件（代码直接嵌入 markdown）
- ❌ 不在 README 里写"用于 X 流程 / 修复 Y"等会随时间过期的元信息

---
id: 0437
title: 路径总和 III
difficulty: 中等
url: https://leetcode.cn/problems/path-sum-iii/
tags: [tree_path]
---

# 路径总和 III

## 题面

给你二叉树的根节点 `root` 和一个整数 `targetSum`，求二叉树中**节点值之和等于 targetSum 的路径的数目**。

路径**不需要从根节点开始，也不需要在叶子节点结束**——但路径方向**必须是向下的**（只能从父到子）。

样例：
- `root = [10,5,-3,3,2,null,11,3,-2,null,1]`、`targetSum = 8` → `3`
- `root = [5,4,8,11,null,13,4,7,2,null,null,5,1]`、`targetSum = 22` → `3`

约束：节点数 `0 ≤ n ≤ 1000`，`-10^9 ≤ Node.val ≤ 10^9`，`-1000 ≤ targetSum ≤ 1000`。
⚠️ 节点值大小可能使**路径和溢出 int32**，但 Go 的 `int` 在 64 位平台是 64 位，足够。

## 思路 1（朴素 O(n²)）：嵌套 DFS

两层 DFS：

- **外层** `dfs`：前序遍历所有节点，把每个节点都当作"路径起点"
- **内层** `countFrom`：从指定起点出发往下走，用 path 回溯方式累加和，每次 `path == targetSum` 时 `count++`

```go
func pathSum(root *TreeNode, targetSum int) int {
    count := 0
    path := 0

    var countFrom func(*TreeNode)
    countFrom = func(node *TreeNode) {
        if node == nil { return }
        path += node.Val
        if path == targetSum { count++ }
        countFrom(node.Left)
        countFrom(node.Right)
        path -= node.Val             // 回溯
    }

    var dfs func(*TreeNode)
    dfs = func(node *TreeNode) {
        if node == nil { return }
        countFrom(node)              // 把当前节点作为路径起点
        dfs(node.Left)
        dfs(node.Right)
    }

    dfs(root)
    return count
}
```

### 关键检查

- `path` 在 `countFrom` 跑完后自动归 0（加多少减多少）
- 外层 `dfs` 不动 `path`，两次 `countFrom` 之间 path 始终 0 → 每个新起点都是干净的

### 浪费在哪？

同一条路径段可能被多次累加：路径 `A→B→C` 中，A 跑 `countFrom` 时算了 A 到 C，B 跑时又算了一遍 B 到 C……重复劳动。**O(n²)** 来自于这种重复。

## 思路 2（O(n) 优化）：前缀和 + 哈希表

待补——思路已通，明天 AC 后回来更新代码。

核心 idea：用 `prefSum: 前缀和 → 出现次数` 哈希表记录**当前祖先链上**所有前缀和。每到一个节点 `D`，查 `prefSum[pref(D) - targetSum]` 就是「以 D 结尾的合法路径数」。哈希表用回溯方式维护——进入加 1、离开减 1。

## 复杂度

### 思路 1 朴素

- **时间：O(n²)**——外层 O(n)，内层每节点最坏走全树 O(n)
- **空间：O(h)**——递归栈 + `path` 标量

### 思路 2 前缀和优化

- **时间：O(n)**——一次 DFS，每节点 O(1) 哈希查询
- **空间：O(h)**——递归栈 + 哈希表（最多存 h 个前缀和，等于当前祖先链长度）

---
id: 0617
title: 合并二叉树
difficulty: 简单
url: https://leetcode.cn/problems/merge-two-binary-trees/
tags: [tree_subtree, tree_dfs]
---

# 合并二叉树

## 题面

给你两棵二叉树 `root1` 和 `root2`。合并成一棵新二叉树：
- 两个节点重叠 → 合并后节点值 = 两值**相加**
- 只有一棵树有该位置节点 → **保留它**

返回合并后的二叉树（**可原地修改**，不必新建）。

样例：
- `root1 = [1,3,2,5]`、`root2 = [2,1,3,null,4,null,7]` → `[3,4,5,5,4,null,7]`
- 两棵都空 → `[]`

约束：节点数 `0 ≤ n ≤ 2000`，`-10^4 ≤ Node.val ≤ 10^4`。

## 思路

**Pattern：tree_subtree 双树同步递归 · 构造类**——结合了 BST 修改类「父节点 = 子调用返回值」的修剪树指针套路（详见 `patterns/bst.md` 心法 #1）。

### 三问（双树版本）

| 问题 | 答案 |
|---|---|
| 1. 递归返回什么？ | `*TreeNode`，合并后的子树根 |
| 2. base case？ | `root1 == nil` → 返回 `root2`；`root2 == nil` → 返回 `root1`（含都 nil 的情况） |
| 3. 当前层做什么？ | 两个都非 nil 时：原地把 root1.Val 加上 root2.Val，递归合并左右子树，**返回值赋回 root1.Left / root1.Right** |

### base case 的"双重作用"

```go
if root1 == nil { return root2 }
if root2 == nil { return root1 }
```

这两行**同时处理三种情况**：

| root1 | root2 | 返回 |
|---|---|---|
| nil | nil | nil（第一个 if 返回 root2=nil）|
| nil | 非 nil | root2 |
| 非 nil | nil | root1（第二个 if）|

精简到极致——**不需要单独处理"都 nil"的情况**。

## 完整代码

```go
func mergeTrees(root1 *TreeNode, root2 *TreeNode) *TreeNode {
    if root1 == nil {
        return root2
    }
    if root2 == nil {
        return root1
    }
    root1.Val += root2.Val
    root1.Left = mergeTrees(root1.Left, root2.Left)
    root1.Right = mergeTrees(root1.Right, root2.Right)
    return root1
}
```

**8 行**搞定，三处亮点：

1. **base case** 用"非 nil 那个直接返回"覆盖三种情况
2. **原地修改 root1**——不新建节点（题目允许）
3. **`root1.Left = mergeTrees(...)`** 是「父节点 = 子调用返回值」模式——把合并后的子树根接回 root1

## 复杂度

- **时间：O(min(n, m))**——只递归到两棵树**都非 nil** 的位置；任一为 nil 立刻返回，不再深入。所以走的节点数 ≤ 两棵树共有的节点数 ≤ min(n, m)。
- **空间：O(min(h1, h2))**——递归栈深度同上理由，h1、h2 分别是两棵树的高度。

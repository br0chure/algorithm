---
id: 0106
title: 从中序与后序遍历序列构造二叉树
difficulty: 中等
url: https://leetcode.cn/problems/construct-binary-tree-from-inorder-and-postorder-traversal/
tags: [tree_construction]
---

# 从中序与后序遍历序列构造二叉树

## 题面

给定两个整数数组 `inorder` 和 `postorder`：
- `inorder` 是某二叉树的**中序遍历**
- `postorder` 是同一棵树的**后序遍历**

构造并返回这棵二叉树。

样例：
- `inorder = [9,3,15,20,7]`、`postorder = [9,15,7,20,3]` → 树 `[3,9,20,null,null,15,7]`

约束：节点数 `1 ≤ n ≤ 3000`，数组无重复元素。

## 思路

**Pattern：tree_construction · 后序定根 + 中序切分**（详见 `patterns/二叉树/tree_construction.md`）。

**106 是 105 的镜像版本**——唯一区别在"根从哪里取"：

| 题 | 根的来源 |
|---|---|
| 105 前+中 | preorder **第一个** 元素 |
| 106 中+后 | postorder **最后一个** 元素 |

其余切分逻辑完全一样。

### 三问

| 问题 | 答案 |
|---|---|
| 1. 递归返回什么？ | `*TreeNode` |
| 2. base case？ | 数组为空 → `nil` |
| 3. 当前层做什么？ | `postorder[-1]` 当根；inorder 找位置切分；递归左右 |

### 关键索引推导

设 `index = root.Val 在 inorder 中的位置`，**左子树有 `index` 个节点**：

| 范围 | 切片表达 |
|---|---|
| 左子树中序 | `inorder[:index]` |
| 右子树中序 | `inorder[index+1:]` |
| 左子树后序 | `postorder[:index]`（长度 = index）|
| 右子树后序 | `postorder[index : len(postorder)-1]`（**注意去掉最后的根**） |

**最容易踩的坑**：右子树后序的结尾要写 `len(postorder)-1`——不写 `-1` 就把根本身又算进了右子树后序里，错误。

## 完整代码

```go
func buildTree(inorder []int, postorder []int) *TreeNode {
    if len(inorder) == 0 {
        return nil
    }
    rootVal := postorder[len(postorder)-1]
    root := &TreeNode{Val: rootVal}

    index := 0
    for inorder[index] != rootVal {
        index++
    }

    root.Left = buildTree(inorder[:index], postorder[:index])
    root.Right = buildTree(inorder[index+1:], postorder[index:len(postorder)-1])
    return root
}
```

## 跟踪样例

```
inorder   = [9, 3, 15, 20, 7]
postorder = [9, 15, 7, 20, 3]

buildTree([9,3,15,20,7], [9,15,7,20,3])
  rootVal = postorder[4] = 3
  index = 1   (3 在 inorder 的位置)
  Left  = buildTree([9],     postorder[:1]=[9])             → 单节点 9
  Right = buildTree([15,20,7], postorder[1:4]=[15,7,20])
            rootVal = 20
            index = 1
            Left  = buildTree([15], [15])                   → 单节点 15
            Right = buildTree([7],  [7])                    → 单节点 7
          → 节点 20 with [15, 7]
```

## 与 105 对比记忆

骨架完全一致，只换两处：

| | 105 | 106 |
|---|---|---|
| 根从哪里取 | `preorder[0]` | `postorder[len(postorder)-1]` |
| 左子树辅助数组 | `preorder[1:1+index]` | `postorder[:index]` |
| 右子树辅助数组 | `preorder[index+1:]` | `postorder[index : len(postorder)-1]` |

**通用心法**（已在 patterns/二叉树/tree_construction.md）：
- 根的位置由"非中序的那一个"决定
- 在中序找根 → 切左右
- 用左子树节点数推另一个数组的切分点

## 复杂度

- **时间：O(n²)**——每次递归 O(n) 找根 + O(n) 切片传递；最坏链状 O(n²)。哈希表优化后 O(n)。
- **空间：O(n)**——递归栈 O(h)；切片是 header（只是窗口），不真的复制底层数组。最终所有切片共享原 inorder / postorder 的底层数据。

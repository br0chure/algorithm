---
id: 0111
title: 二叉树的最小深度
difficulty: 简单
url: https://leetcode.cn/problems/minimum-depth-of-binary-tree/
tags: [tree_dfs]
---

# 二叉树的最小深度

## 题面

给定一棵二叉树，返回它的**最小深度**——从根节点到**最近叶子节点**的最短路径上的节点数。

> ⚠️ 叶子节点 = **左右孩子都为 nil** 的节点。中间单孩子节点**不是叶子**。

样例：
- `[3,9,20,null,null,15,7]` → `2`（路径：3 → 9）
- `[2,null,3,null,4,null,5,null,6]`（链状）→ `5`
- `[]` → `0`

约束：节点数 `0 ≤ n ≤ 10^5`，`-1000 ≤ Node.val ≤ 1000`。

## 思路

**Pattern：tree_dfs · Type A（返回值即答案）+ 边界处理**。

### 核心三问

| 问题 | 答案 |
|---|---|
| 1. 递归返回什么？ | `int`，「子树最小深度（节点数）」 |
| 2. 空节点返回什么？ | `0` |
| 3. 当前节点拿到 left、right 后做什么？ | **合并时过滤 0**：哪一侧是 0（nil），就不参与 min；都非 0 取 min |

### 关键陷阱：为什么不能直接 `min(left, right) + 1`

考虑这棵树：

```
1
 \
  2
```

- `minDepth(1.Left)` = 0（nil 子树）
- `minDepth(1.Right)` = 1（叶子节点 2）
- 直觉版 `min(0, 1) + 1` = **1** ❌

但答案应该是 `2`（路径 1 → 2，2 个节点）。问题在于：**「`minDepth(nil) = 0`」这个约定，和「合法子树最小深度 ≥ 1」的语义撞车了**——`0` 既能表示「这一侧不存在」也能被错误当成「这一侧深度极小」。

题面的关键约束是「**到叶子**的最短路径」。节点 1 的左孩子是 nil，**没有叶子**，所以左侧那条「路径」根本不存在，不能参与比较。必须强制走右侧。

## 完整代码

```go
func minDepth(root *TreeNode) int {
    if root == nil {
        return 0
    }
    left := minDepth(root.Left)
    right := minDepth(root.Right)

    if left == 0 && right != 0 {
        return right + 1
    }
    if left != 0 && right == 0 {
        return left + 1
    }
    if left == 0 && right == 0 {
        return 1
    }
    return min(left, right) + 1
}
```

### 简化写法（取巧但等价）

利用「`0 + x = x`」的加法身份元：

```go
if left == 0 || right == 0 {
    return left + right + 1   // 一个非零就返非零那个+1；两个都零就返 1
}
return min(left, right) + 1
```

正确性验证：

| 情况 | left + right + 1 | 期望 |
|---|---|---|
| left=0, right=5 | 0+5+1=6 | 6 ✓ |
| left=3, right=0 | 3+0+1=4 | 4 ✓ |
| 双 0（叶子） | 0+0+1=1 | 1 ✓ |

**两种等价**。前者语义清晰、易读；后者紧凑但依赖加法身份元的"取巧"。归档保留前者。

### 与 104 的本质差异

| | 104 最大深度 | 111 最小深度 |
|---|---|---|
| Type | A | A（含边界） |
| 合并 | `max(left, right) + 1` | 合并要**过滤 0** |
| 为什么 104 不需要过滤？ | `max(left, right)` 永远会选非零那侧（因为合法值 ≥ 1 > 0）→ **0 自动被忽略** | `min(left, right)` 会让 0 胜出 → 必须显式过滤 |

> **更通用的心法**：当 `base case` 返回值（如 0）和「合法值的语义」可能撞车时，合并步骤必须显式区分「nil 不存在」vs「合法值很小」。`max` 类聚合天然规避，`min` 类聚合要警觉。

## 复杂度

- **时间：O(n)**——每个节点恰好被递归访问 1 次，节点级动作 O(1)。
- **空间：O(h)**——递归栈，h 为树高（最坏链状 O(n)、平衡 O(log n)）。

  栈帧分析（详见 `patterns/tree_dfs.md` 心法 #1）：单帧装 `root` 指针 + `left`、`right` 两个 int + 元信息，O(1)。函数体没有动态分配，单帧不随 n 变化。

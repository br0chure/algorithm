---
id: 0105
title: 从前序与中序遍历序列构造二叉树
difficulty: 中等
url: https://leetcode.cn/problems/construct-binary-tree-from-preorder-and-inorder-traversal/
tags: [tree_construction]
---

# 从前序与中序遍历序列构造二叉树

## 题面

给定两个整数数组 `preorder` 和 `inorder`：
- `preorder` 是某二叉树的**前序遍历**
- `inorder` 是同一棵树的**中序遍历**

构造并返回这棵二叉树。

样例：
- `preorder = [3,9,20,15,7]`、`inorder = [9,3,15,20,7]` → 树 `[3, 9, 20, null, null, 15, 7]`
- `preorder = [-1]`、`inorder = [-1]` → 单节点

约束：节点数 `1 ≤ n ≤ 3000`，preorder / inorder **无重复元素**。

## 思路

**Pattern：tree_construction · 前序定根 + 中序切分**（详见 `patterns/tree_construction.md`）。

### 两种遍历的关键性质

| 遍历 | 顺序 | 性质 |
|---|---|---|
| 前序 | 根 → 左 → 右 | **第一个元素必然是根** |
| 中序 | 左 → 根 → 右 | 根**左侧**全是左子树，**右侧**全是右子树 |

### 三问

| 问题 | 答案 |
|---|---|
| 1. 递归返回什么？ | `*TreeNode`，新建子树的根 |
| 2. base case？ | 数组为空 → `nil` |
| 3. 当前层做什么？ | `preorder[0]` 当根，在 `inorder` 找根的位置切分，递归左右 |

### 关键索引推导

设根在 inorder 中位置为 `index`，则**左子树有 `index` 个节点**：

| 范围 | 切片表达 |
|---|---|
| 左子树前序 | `preorder[1 : 1+index]`（紧跟根之后，长度 = index）|
| 左子树中序 | `inorder[:index]` |
| 右子树前序 | `preorder[index+1 :]` |
| 右子树中序 | `inorder[index+1 :]` |

## 完整代码

```go
func buildTree(preorder []int, inorder []int) *TreeNode {
    if len(preorder) == 0 {
        return nil
    }
    root := &TreeNode{Val: preorder[0]}
    index := getIndex(inorder, root.Val)
    root.Left = buildTree(preorder[1:index+1], inorder[:index])
    root.Right = buildTree(preorder[index+1:], inorder[index+1:])
    return root
}

func getIndex(inorder []int, val int) int {
    for i, v := range inorder {
        if v == val {
            return i
        }
    }
    return 0   // 题目保证 val 一定在 inorder 中，这里不会执行
}
```

## 跟踪样例

```
preorder = [3, 9, 20, 15, 7]
inorder  = [9, 3, 15, 20, 7]

buildTree([3,9,20,15,7], [9,3,15,20,7])
  root = 3
  index = 1   (3 在 inorder 的位置)
  Left  = buildTree([9],      [9])          → 单节点 9
  Right = buildTree([20,15,7], [15,20,7])
            root = 20
            index = 1
            Left  = buildTree([15], [15])   → 单节点 15
            Right = buildTree([7], [7])     → 单节点 7
          → 节点 20 with [15, 7]
```

最终：

```
    3
   / \
  9  20
     / \
   15   7
```

## 优化方向（不必现在做）

`getIndex` 是 O(n) 线性查找——总复杂度退化到 O(n²)。
**优化**：在主函数入口预建 `inorder` 的 `val → index` 哈希表，每次 O(1) 查找，总 O(n)。这是 LC 进阶解，**面试常被问到**，但 AC 不强求。

## 复杂度

- **时间：O(n²)**——每次递归 O(n) 找位置 + O(n) 切片拷贝；最坏链状树 n 层 → O(n²)。平衡树 O(n log n)。哈希表优化后 O(n)。
- **空间：O(n²)**——切片版本每层都创建新切片副本，累计 O(n²)；递归栈 O(h)。索引版本可降到 O(n)。

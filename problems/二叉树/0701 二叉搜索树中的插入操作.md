---
id: 0701
title: 二叉搜索树中的插入操作
difficulty: 中等
url: https://leetcode.cn/problems/insert-into-a-binary-search-tree/
tags: [bst]
---

# 二叉搜索树中的插入操作

## 题面

给定 BST 的根节点 `root` 和要插入树中的值 `value`，将值插入 BST。返回插入后的根节点。

> 保证新值和 BST 中任何现有值都不同。**可能存在多种有效的插入方式，任意一种合法 BST 即可**（LC 用专用 checker 验证）。

约束：节点数 `0 ≤ n ≤ 10^4`，`-10^8 ≤ Node.val, val ≤ 10^8`，val 不在树中存在。

## 思路

**Pattern：bst · 左小右大递归 + 返回值修剪树指针** —— Type A 构造类。

### 关键洞察：新节点永远挂在 nil 处当叶子

沿着 BST 性质比较：

- `val < root.Val` → val 必在左子树范围 → 走左
- `val > root.Val` → val 必在右子树范围 → 走右
- 一直走到 nil → 这就是 val 的归宿，在这里建新节点

整条路径上的现有节点**不需要改动**——只是把新节点挂在某个原本 nil 的左/右孩子位置。

### 三问

| 问题 | 答案 |
|---|---|
| 1. 递归返回什么？ | `*TreeNode`，「插入后的子树新根」（nil 时是新建节点；非 nil 时是 root 本身） |
| 2. base case？ | `root == nil` → 返回 `&TreeNode{Val: val}`（这就是插入位置） |
| 3. 当前节点做什么？ | 按 BST 性质决定方向，**用返回值赋值回 `root.Left` / `root.Right`**——这是把新节点"接回树"的关键 |

## 完整代码

```go
func insertIntoBST(root *TreeNode, val int) *TreeNode {
    if root == nil {
        return &TreeNode{Val: val}
    }
    if val < root.Val {
        root.Left = insertIntoBST(root.Left, val)
    } else {
        root.Right = insertIntoBST(root.Right, val)
    }
    return root
}
```

### 关键 trick：`root.Left = insertIntoBST(...)` 这一行

很多人写成下面这样，**新节点会飘走**：

```go
insertIntoBST(root.Left, val)   // ❌ 没接住返回值
```

走到 nil 处建的新节点确实被返回了，但没人接——没有任何指针指向它，被 gc 回收。

**正确**：

```go
root.Left = insertIntoBST(root.Left, val)   // ✅ 把返回值接回 root.Left
```

两种情况：

- 递归经过的**中间节点**：返回的就是原 `root.Left`，赋值后**等于没动**（无副作用）
- **base case 触发**（root.Left == nil 那次调用）：返回**新建节点**，赋值让新节点真正挂到树上

> 💡 **小心法**（不入 pattern 文档）：树类**修改**题的标准套路是「**父节点用 `=` 接住子调用的返回值**」。700 搜索不需要，因为不改树；701 / 450 等改树类必须用。

## 扩展：split-based 非叶子插入

题目允许多种合法 BST，理论上可以让 val 直接**成为新根**——把原树按 val 切成两部分：< val 的去左、> val 的去右。

```go
// 让 val 当新根：先把原树按 val 切两半，再拼起来
func insertIntoBST_Split(root *TreeNode, val int) *TreeNode {
    left, right := split(root, val)
    return &TreeNode{Val: val, Left: left, Right: right}
}

func split(node *TreeNode, val int) (*TreeNode, *TreeNode) {
    if node == nil { return nil, nil }
    if node.Val < val {
        l, r := split(node.Right, val)
        node.Right = l
        return node, r
    }
    l, r := split(node.Left, val)
    node.Left = r
    return l, node
}
```

代码量是叶子插入的两倍，**LC AC 但不是标准答案**。仅在 AVL / 红黑树 / 持久化 BST 等真有"控制根位置"需求的场景才用。

## 复杂度

- **时间：O(h)**——沿一条路径从根走到 nil 位置。平衡 BST O(log n)，最坏链状 O(n)。
- **空间：O(h)**——递归栈深度等于路径长度。

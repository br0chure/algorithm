---
id: 0094
title: 二叉树的中序遍历
difficulty: 简单
url: https://leetcode.cn/problems/binary-tree-inorder-traversal/
tags: [tree_traversal]
---

# 二叉树的中序遍历

## 题面

给你二叉树的根节点 `root`，返回它节点值的**中序遍历**（左 → 根 → 右）。

样例：
- `root = [1,null,2,3]` → `[1, 3, 2]`
- `root = []` → `[]`
- `root = [1]` → `[1]`

约束：节点数 `0 ≤ n ≤ 100`。

## 主解：迭代版（游标 + 栈）

### 为什么中序迭代是三序里最难的

前序/后序的迭代套路是**"压栈即将访问"**——压进栈就立刻弹出处理。

但中序要**先沉到最左下方才能访问当前节点**——"沉到底"的过程中栈顶不是要立刻访问的节点。所以前/后序的"压完直接弹"模板对中序失效。

**核心套路**：引入一个**游标 `cur`** 配合栈：

- 栈装的是「**还没被访问的祖先**」
- 游标 `cur` 指向「**当前要继续向下探索的节点**」

### 三个动作

1. **一路向左压栈**：每个节点都压进栈，直到 `cur == nil`（沉到最深左孩子的下方）
2. **弹栈处理**：栈顶是当前最左未处理的节点，弹出加入 result
3. **转向右子树**：`cur = cur.Right` 继续处理它的右子树（也是先沉左 → 弹 → 转右）

### 代码

```go
func inorderTraversal(root *TreeNode) []int {
    if root == nil {
        return []int{}
    }
    result := make([]int, 0)
    stack := []*TreeNode{}
    cur := root
    for cur != nil || len(stack) > 0 {
        // ① 一路向左压栈
        for cur != nil {
            stack = append(stack, cur)
            cur = cur.Left
        }
        // ② 弹出最左未处理的节点
        cur = stack[len(stack)-1]
        stack = stack[:len(stack)-1]
        result = append(result, cur.Val)
        // ③ 转向右子树（让外层 for 继续沉左）
        cur = cur.Right
    }
    return result
}
```

### 循环条件 `cur != nil || len(stack) > 0`

**两个条件用 `||` 连**：

- `cur != nil`：还有节点要继续探索（要么沉左，要么处理右子树的根）
- `len(stack) > 0`：栈里还有未处理的祖先

两个都为 false 时才结束——表示「无未探索节点 + 无未处理祖先」。

### 跟踪样例 `[1, null, 2, 3]`

```
1
 \
  2
 /
3
```

| 状态 | cur | stack | result | 动作 |
|---|---|---|---|---|
| 初始 | 1 | [] | [] | |
| ① 沉左 | nil | [1] | [] | 1 入栈，cur 走到 1.Left = nil |
| ② 弹 1，加 result | 1.Right = 2 | [] | [1] | |
| ① 沉左 | nil | [2] | [1] | 2 入栈，cur 走到 2.Left = 3 → 入栈，cur 走到 3.Left = nil |
| 实际 | nil | [2, 3] | [1] | |
| ② 弹 3，加 result | 3.Right = nil | [2] | [1, 3] | |
| ② 弹 2，加 result | 2.Right = nil | [] | [1, 3, 2] | |
| 结束（cur=nil, stack=[]）| | | **[1, 3, 2]** ✓ | |

## 备用解：递归版

```go
func inorderTraversal(root *TreeNode) []int {
    if root == nil {
        return nil
    }
    result := inorderTraversal(root.Left)
    result = append(result, root.Val)
    result = append(result, inorderTraversal(root.Right)...)
    return result
}
```

三行核心逻辑（左 → 中 → 右），和 144/145 一样——递归三序的差别只在 `append(result, root.Val)` 的位置。

## 复杂度

### 迭代版

- **时间：O(n)**——每个节点入栈出栈各 1 次，O(1) 处理。
- **空间：O(h)**——栈装的祖先链最长 h（链状树最差 O(n)）。

### 递归版

- **时间：O(n²)**——切片合并的拷贝累积。
- **空间：O(h)**——递归栈 + result 临时切片 O(n)。

> n ≤ 100 两版都不超时。**迭代版面试更看重**——比递归更能体现对栈和遍历顺序的理解。

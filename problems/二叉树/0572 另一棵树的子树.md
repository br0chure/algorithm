---
id: 0572
title: 另一棵树的子树
difficulty: 简单
url: https://leetcode.cn/problems/subtree-of-another-tree/
tags: [tree_subtree, tree_dfs]
---

# 另一棵树的子树

## 题面

给你两棵二叉树 `root` 和 `subRoot`。判断 `root` 中是否**包含**和 `subRoot` 具有相同结构和节点值的子树。

> 「子树」= 某个节点 + 它的所有后代节点。`root` 自身也算它的子树。

样例：
- `root = [3,4,5,1,2]`、`subRoot = [4,1,2]` → `true`（root 中以节点 4 为根的子树就是 subRoot）
- `root = [3,4,5,1,2,null,null,null,null,0]`、`subRoot = [4,1,2]` → `false`（root 中的 4 子树多了个 0）

约束：root 节点数 `1 ≤ n ≤ 2000`，subRoot 节点数 `1 ≤ m ≤ 1000`，`-10^4 ≤ Node.val ≤ 10^4`。

## 思路

**Pattern：双重递归**——外层单树 DFS 找位置，内层双树同步递归比较。

### 关键洞察：两层不同的递归

| 层 | 函数 | 类型 | 任务 |
|---|---|---|---|
| 外层 | `isSubtree(root, subRoot)` | 单树 DFS（Type A bool 变体） | 在 root 里走每个节点位置，问"以这里为根的子树是否等于 subRoot" |
| 内层 | `isSameTree(p, q)` | 双树同步递归 · 同向配对 | 判断两棵树是否完全相同（即 100 题的解法） |

> ⚠️ **判别细节**：外层 `isSubtree` 虽然函数签名带两个参数，但 **subRoot 在递归中始终不变**——所以**不算双树同步递归**，是「单树 DFS + 一个不变的引用参数」。详见 `patterns/二叉树/tree_subtree.md` 心法对「双树同步递归」的精确定义。

### 三问（外层 isSubtree）

| 问题 | 答案 |
|---|---|
| 1. 递归返回什么？ | `bool`，「以当前 root 为根的子树**任意位置**是否包含 subRoot」 |
| 2. 空节点返回什么？ | `false`（题目约束 subRoot ≥ 1 节点，nil 容不下） |
| 3. 当前节点拿到 left、right 后做什么？ | **3 种 true 情况**：root 自己就是 subRoot 的相同树 OR 左子树包含 OR 右子树包含 |

## 完整代码（短路版主解）

```go
func isSubtree(root, subRoot *TreeNode) bool {
    if root == nil {
        return false
    }
    return isSameTree(root, subRoot) ||
        isSubtree(root.Left, subRoot) ||
        isSubtree(root.Right, subRoot)
}

func isSameTree(p, q *TreeNode) bool {
    if p == nil && q == nil {
        return true
    }
    if p == nil || q == nil {
        return false
    }
    if p.Val != q.Val {
        return false
    }
    return isSameTree(p.Left, q.Left) && isSameTree(p.Right, q.Right)
}
```

### 短路 vs 全算

首次 AC 时容易写成这种「先全算再 OR」：

```go
left := isSubtree(root.Left, subRoot)
right := isSubtree(root.Right, subRoot)
mid := isSameTree(root, subRoot)
return left || right || mid
```

**功能等价**，但**失去了短路效果**——三个表达式都先算完再 OR，找到匹配也不会提前退出。
直接连写 `a || b || c` 才能利用 Go 的 `||` 短路：找到第一个 true 立刻返回。

> **小心法**：`&&` / `||` 在表达式中**自动短路**，但**赋值到变量后失效**。需要短路时直接连写。

## 与 100 / 101 对比

| | 100 相同的树 | 101 对称二叉树 | 572 另一棵树的子树 |
|---|---|---|---|
| 结构 | 单层双树同步 | 单层双树同步 | **双重递归** |
| 配对方式 | 同向 | 交叉（镜像） | 内层用同向 |
| 子树比较器 | 自己就是 | 自己就是 | 借用 100 的 `isSameTree` |
| Helper 设计 | LC 签名 `(p, q)` 直接递归 | 闭包桥接单参入口 | **两个独立函数**：外层找位置 + 内层比较 |

> **掌握度自检**：能否秒答这个变体——「**root 是否包含 subRoot 作为镜像子树**」？
> 提示：外层 isSubtree 不变；内层把 100 isSameTree 换成 101 isMirror（左对右、右对左）。

## 复杂度

- **时间：O(n × m)**
  - 外层在 root 的每个节点（n 个）都尝试一次 isSameTree
  - 每次 isSameTree 最坏遍历整个 subRoot（m 个节点）
  - 总 O(n × m)；命中早可以提前退出
- **空间：O(h_root + h_subRoot)** ≈ **O(h)**
  - 外层 isSubtree 递归栈 O(h_root)
  - 内层 isSameTree 递归栈 O(h_subRoot)
  - 两层不同时活到最深（外层先递归到底，再调内层）→ 实际峰值是两者之和量级

---
id: 0257
title: 二叉树的所有路径
difficulty: 简单
url: https://leetcode.cn/problems/binary-tree-paths/
tags: [tree_path]
---

# 二叉树的所有路径

## 题面

给你一个二叉树的根节点 `root`，按任意顺序返回所有**从根节点到叶子节点**的路径。

路径格式：用 `"->"` 连接，如 `"1->2->5"`。

样例：
- `[1,2,3,null,5]` → `["1->2->5", "1->3"]`
- `[1]` → `["1"]`

约束：节点数 `1 ≤ n ≤ 100`，`-100 ≤ Node.val ≤ 100`。

## 思路

**Pattern：tree_path · DFS + 回溯** —— `tree_path` pattern 的入门题。

### 核心：维护"当前路径栈" path

`path` 永远反映「从根到当前正在访问的节点」的路径。三个操作配对出现：

1. **进入节点**：把 Val 推进 path
2. **叶子节点**：把 path 拼成字符串加入 result
3. **离开节点**：把 Val 从 path 弹出（**回溯**）

> ⚠️ 没有第 3 步会怎样？处理完左子树的路径 `1→2` 后，path 残留 `["1","2"]`，进入右子树节点 3 时变成 `["1","2","3"]`——污染了下一条路径。**回溯保证 path 永远是"当前的路径"**。

### 三问

| 问题 | 答案 |
|---|---|
| 1. 递归返回什么？ | `void`——答案是"路径集合"（多条路径），单值返回装不下，所以用外部 `result` 收集 |
| 2. base case？ | `node == nil` → return |
| 3. 当前节点做什么？ | 进入：append Val 到 path → 叶子收集 / 非叶子递归 → 离开：弹出 |

### 关于"返回什么"的判断

DFS 题选返回值策略的核心问题：

> 「**单次递归调用能不能产出一个单一的"答案片段"？**」

- **能**（如深度、最大路径和） → 返回值（策略 A）
- **不能**（答案是一个集合：所有路径、所有组合） → 返回 void + 外部 result 收集（策略 B）

257 答案是路径**集合**，所以用策略 B（回溯模板）。

## 完整代码

```go
import (
    "strconv"
    "strings"
)

func binaryTreePaths(root *TreeNode) []string {
    result := []string{}
    path := []string{}
    var dfs func(*TreeNode)
    dfs = func(node *TreeNode) {
        if node == nil {
            return
        }
        path = append(path, strconv.Itoa(node.Val))   // 进入：加入

        if node.Left == nil && node.Right == nil {
            result = append(result, strings.Join(path, "->"))   // 叶子：收集
        } else {
            dfs(node.Left)
            dfs(node.Right)
        }

        path = path[:len(path)-1]                     // 离开：弹出（回溯）
    }
    dfs(root)
    return result
}
```

### 注意：这里不需要拷贝 path

通常回溯收集时需要 `append([]int{}, path...)` 拷贝——因为 path 后续会变（共享底层数组）。

但 257 收集的是 `strings.Join(path, "->")` 返回的**新字符串**——`Join` 已经做了**拷贝**（生成一个新的 string，不共享 path 的底层）。所以**不需要额外拷贝**。

如果收集的是 `[]int`（如 113），就需要显式拷贝。

## 跟踪样例 `[1,2,3,null,5]`

```
   1
  / \
 2   3
  \
   5
```

| 步骤 | path | result |
|---|---|---|
| 进入 1 | `["1"]` | `[]` |
| 进入 2 | `["1","2"]` | `[]` |
| 进入 5（叶子）| `["1","2","5"]` | `["1->2->5"]` |
| 离开 5 | `["1","2"]` | `["1->2->5"]` |
| 离开 2 | `["1"]` | `["1->2->5"]` |
| 进入 3（叶子）| `["1","3"]` | `["1->2->5","1->3"]` |
| 离开 3 | `["1"]` | `["1->2->5","1->3"]` |
| 离开 1 | `[]` | `["1->2->5","1->3"]` |

## 复杂度

- **时间：O(n²)**——每个节点访问 1 次（O(n)）；叶子节点 `strings.Join(path, "->")` 花 O(h)（路径长度）。最坏满二叉树有 n/2 个叶子，每个 join 是 O(log n) → 总 O(n log n)；最坏链状只 1 个叶子，path 长 n → O(n)。综合最坏 O(n²)。
- **空间：O(h)**——递归栈 + path 栈，h 为树高。result 不计入辅助空间。

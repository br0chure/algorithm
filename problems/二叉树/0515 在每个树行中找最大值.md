---
id: 0515
title: 在每个树行中找最大值
difficulty: 中等
url: https://leetcode.cn/problems/find-largest-value-in-each-tree-row/
tags: [tree_bfs]
---

# 在每个树行中找最大值

## 题面

给定一棵二叉树的根节点 `root`，返回每一行中最大的节点值（即按层遍历，每层取 max）。

样例：
- 输入 `root = [1,3,2,5,3,null,9]` → 输出 `[1, 3, 9]`
- 输入 `root = []` → 输出 `[]`

约束：节点数 `0 ≤ n ≤ 10^4`，`-2^31 ≤ Node.val ≤ 2^31 - 1`。

## 思路

**Pattern：树的 BFS（聚合型）**。模板和 102 一样，差别在「每层不收集所有 Val、只维护一个 max」。

### 关键拆解：「这个变量属于哪一层？」

`levelMax` 的语义是 **「本层的最大值」**——和**层**绑定，不和**节点**绑定。
按「初始化位置 = 重置频率 = 语义」原则，它必须**在外层 for 的开头、内层 for 之前**初始化。

### 初值的两种写法

| 写法 | 优点 | 缺点 |
|---|---|---|
| `levelMax := math.MinInt` | 无脑通用 | 依赖 `math` 包；理解 `MinInt` 是负数（`-2^63`） |
| `levelMax := queue[0].Val` | **不依赖任何哨兵**，节点值范围再变也不出错 | 仅在「集合非空」时可用（这里 `levelSize ≥ 1`，恒成立） |

本解法采用第二种：用「集合中已有元素」做初值，**永远不会被边界值坑到**。

### 与「收集型」BFS 的差异（聚合 vs 收集）

| 题型 | 每层维护 | 内层动作 | 层尾动作 |
|---|---|---|---|
| 102 收集 | `level []int` | `level = append(level, node.Val)` | `result = append(result, level)` |
| 515 聚合 | `levelMax int` | `if node.Val > levelMax { ... }` | `result = append(result, levelMax)` |

聚合型不需要中间切片，**直接在标量上滚动维护**即可，省内存也更直接。

## 完整代码

```go
func largestValues(root *TreeNode) []int {
    result := make([]int, 0)
    if root == nil {
        return result
    }
    queue := []*TreeNode{root}

    for len(queue) > 0 {
        levelSize := len(queue)
        levelMax := queue[0].Val // 用本层第一个节点的 Val 做初值，免去哨兵
        for i := 0; i < levelSize; i++ {
            node := queue[0]
            queue = queue[1:]
            if node.Val > levelMax {
                levelMax = node.Val
            }
            if node.Left != nil {
                queue = append(queue, node.Left)
            }
            if node.Right != nil {
                queue = append(queue, node.Right)
            }
        }
        result = append(result, levelMax)
    }
    return result
}
```

## 复杂度

- **时间：O(n)**
  - 每个节点**恰好出队一次、入队一次**（根节点入队是初始化时的一次）。
  - 节点级动作（比较 `levelMax`、入队左右孩子）都是 O(1)。
  - `queue = queue[1:]` 是切片头部「丢弃」，不复制底层数组，O(1)。
  - 总操作数 = 节点数 × 常数 = O(n)。

- **空间：O(n)**（具体由队列峰值决定）
  - **队列**：BFS 队列在某个瞬间会同时持有「下一层全部节点」。最坏情况是**满二叉树**——
    - 满二叉树各层节点数：1, 2, 4, …, 2ʰ，总数 `n = 2^(h+1) - 1`。
    - 最后一层节点数 `2ʰ ≈ n/2`（前面所有层加起来 `2ʰ - 1`，比最后一层只少 1）。
    - 因此队列峰值 ≈ `n/2`，量级 **O(n)**。
  - **`result` 切片**：每层只追加一个 `int`，共 `O(树高 h)`。
    - 平衡树时 `h = O(log n)`；最坏链状树时 `h = O(n)`。
    - 但 `h ≤ 队列峰值的量级`，不构成新的瓶颈。
  - **标量变量**（`levelSize`、`levelMax`、`node`、`i`）：O(1)。
  - 综合：**空间瓶颈来自队列，O(n)**。
  - （注：是否计入 `result` 通常按惯例**不计**，因为它是输出本身、必须返回。即使计入也不改变量级。）

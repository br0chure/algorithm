---
id: 0144
title: 二叉树的前序遍历
difficulty: 简单
url: https://leetcode.cn/problems/binary-tree-preorder-traversal/
tags: [tree_traversal]
---

# 二叉树的前序遍历

## 题面

给你二叉树的根节点 `root`，返回它节点值的**前序遍历**（根 → 左 → 右）。

样例：
- `root = [1,null,2,3]` → `[1, 2, 3]`
- `root = []` → `[]`
- `root = [1]` → `[1]`

约束：节点数 `0 ≤ n ≤ 100`。

## 主解：迭代版（显式栈模拟递归）

### 核心思路

栈是 LIFO（后入先出）。前序要 `根 → 左 → 右`，**要让左孩子先被弹出**——所以入栈时**先入右、后入左**。

### 三问（迭代版）

| 问题 | 答案 |
|---|---|
| 1. 栈装什么？ | `*TreeNode` |
| 2. 每轮做什么？ | 弹栈顶 → 加入 result → 推入孩子 |
| 3. 左右入栈顺序？ | 先右后左（让左在栈顶，下次先弹出）|

### 代码

```go
func preorderTraversal(root *TreeNode) []int {
    if root == nil {
        return []int{}
    }
    result := make([]int, 0)
    stack := []*TreeNode{root}
    for len(stack) > 0 {
        node := stack[len(stack)-1]
        stack = stack[:len(stack)-1]      // ⚠️ 弹出（不能漏）
        result = append(result, node.Val)
        if node.Right != nil {
            stack = append(stack, node.Right)   // 先右
        }
        if node.Left != nil {
            stack = append(stack, node.Left)    // 后左 → 栈顶 → 下次先弹
        }
    }
    return result
}
```

### 易错点（已踩过的坑）

| 错误 | 后果 |
|---|---|
| 漏写 `stack = stack[:len(stack)-1]` | 死循环（栈顶永远不变） |
| 入栈写成 `root.Right / root.Left` 而不是 `node.Right / node.Left` | 永远在塞根的孩子，死循环 |

这两个坑是「**写循环时 `root` vs `node` 混淆**」的体现——之前刷 0199 也踩过。

> **小防御技巧**：写循环时，**先把 `root` 这个名字"丢掉"**——只剩 `node` 在脑子里。所有"对当前节点的操作"都用 `node`。

## 备用解：递归版（Type A 自递归合并）

```go
func preorderTraversal(root *TreeNode) []int {
    if root == nil {
        return nil
    }
    result := []int{root.Val}
    result = append(result, preorderTraversal(root.Left)...)
    result = append(result, preorderTraversal(root.Right)...)
    return result
}
```

**思路**：函数返回这棵子树的前序遍历列表。父层把左/右孩子返回的列表拼到自己 Val 之后。完全 Type A 风格（返回值就是答案），不需要 helper / 闭包 / 指针参数。

5 行，是最优雅的递归版。

### 其他写法

- **指针参数版**：定义 helper `func preorder(node *TreeNode, result *[]int)`，把外层 `result` 的地址传进去，递归时 `*result = append(*result, ...)`。O(n) 时间，但代码长 1 倍。
- **闭包版**：外层 `result := []int{}`，内层 `dfs` 闭包修改 `result`。O(n) 时间。和指针版等价。

n ≤ 100 用任何版本都不超时——**自递归合并版 5 行最干净**。

## 复杂度

### 迭代版

- **时间：O(n)**——每个节点入栈+出栈各 1 次。
- **空间：O(h)**——栈最多装 h 个节点（沿一条路径下沉时）。

### 递归版（自递归合并）

- **时间：O(n²)** ——每层 append 子列表是 O(子列表长度)，累积 O(n²)。
- **空间：O(h)** for 递归栈 + O(n) for result 临时切片。

> 如果 n 很大要选 O(n) 时间方案：迭代版或指针参数版。

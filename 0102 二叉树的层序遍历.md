---
id: 0102
title: 二叉树的层序遍历
difficulty: 中等
url: https://leetcode.cn/problems/binary-tree-level-order-traversal/
tags: [tree_bfs]
---

# 二叉树的层序遍历

## 题面

给你二叉树的根节点 `root`，返回其节点值的**层序遍历**结果（即逐层地，从左到右访问所有节点）。

样例：
- 输入 `root = [3,9,20,null,null,15,7]` → 输出 `[[3], [9,20], [15,7]]`
- 输入 `root = [1]` → 输出 `[[1]]`
- 输入 `root = []` → 输出 `[]`

约束：节点数 `0 ≤ n ≤ 2000`，`-1000 ≤ Node.val ≤ 1000`。

## 思路

**Pattern：树的 BFS（广度优先 / 层序遍历）**，标准模板题。

1. 用队列（slice 模拟）存放待访问节点，初始放入根。
2. 每轮循环**先 `size := len(queue)` 锁定本层节点数**，避免边出边入混淆层级。
3. 内层循环弹 `size` 次：节点 `Val` 进当前层切片，左右子（非空）入队。
4. 一层处理完，把 `level` 追加进 `result`。
5. 边界：`root == nil` 直接返回空切片。

> 关键技巧：`size := len(queue)` 是分层的"分界线"，是树 BFS 模板的核心。后续 `199. 二叉树的右视图`、`107. 层序遍历 II`、`103. 锯齿形层序遍历` 都是它的小变体。

## 完整代码

```go
func levelOrder(root *TreeNode) [][]int {
    if root == nil {
        return [][]int{}
    }
    var result [][]int
    queue := []*TreeNode{root}
    for len(queue) > 0 {
        size := len(queue)
        level := make([]int, 0, size)
        for i := 0; i < size; i++ {
            node := queue[0]
            queue = queue[1:]
            level = append(level, node.Val)
            if node.Left != nil {
                queue = append(queue, node.Left)
            }
            if node.Right != nil {
                queue = append(queue, node.Right)
            }
        }
        result = append(result, level)
    }
    return result
}
```

## 复杂度

- 时间：O(n)，每个节点恰好出入队列一次。
- 空间：O(n)，队列在最坏情况下（满二叉树最后一层）需存约 n/2 个节点。
